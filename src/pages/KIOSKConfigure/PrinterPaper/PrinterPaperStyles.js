import { StyleSheet } from 'react-native';

const PrinterPaperStyles = StyleSheet.create({
    containter: { flex: 1, alignItems: 'center', justifyContent: 'center' },
});

export default PrinterPaperStyles;
