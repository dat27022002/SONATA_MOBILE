import { StyleSheet } from 'react-native';

const RealTimeSaleStyles = StyleSheet.create({
    dateRangePicker: { flexDirection: 'row', flex: 1, alignItems: 'center' },
});

export default RealTimeSaleStyles;
