import { StyleSheet } from 'react-native';

const WeeklySaleStyles = StyleSheet.create({
    dateRangePicker: { flexDirection: 'row', flex: 1, alignItems: 'center' },
});

export default WeeklySaleStyles;
