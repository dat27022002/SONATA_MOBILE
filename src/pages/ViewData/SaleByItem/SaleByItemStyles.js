import { StyleSheet } from 'react-native';

const SaleByItemStyles = StyleSheet.create({
    dateRangePicker: { flexDirection: 'row', flex: 1, alignItems: 'center' },
});

export default SaleByItemStyles;
