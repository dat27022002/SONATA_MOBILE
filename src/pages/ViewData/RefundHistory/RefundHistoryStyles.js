import { StyleSheet } from 'react-native';

const RefundHistoryStyles = StyleSheet.create({
    dateRangePicker: { flexDirection: 'row', flex: 1, alignItems: 'center' },
});

export default RefundHistoryStyles;
