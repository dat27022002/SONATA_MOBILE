import { StyleSheet } from 'react-native';

const InventoryStatusStyles = StyleSheet.create({
    dateRangePicker: { flexDirection: 'row', flex: 1, alignItems: 'center' },
    viewIncoudeLack: {
        flexDirection: 'row',
        alignItems: 'center',
        justifyContent: 'flex-start',
        flex: 1,
        marginLeft: -8,
    },
});

export default InventoryStatusStyles;
