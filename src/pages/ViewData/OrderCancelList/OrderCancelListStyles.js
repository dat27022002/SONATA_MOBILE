import { StyleSheet } from 'react-native';

const OrderCancelListStyles = StyleSheet.create({
    dateRangePicker: { flexDirection: 'row', flex: 1, alignItems: 'center' },
});

export default OrderCancelListStyles;
