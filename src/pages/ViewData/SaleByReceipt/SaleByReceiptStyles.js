import { StyleSheet } from 'react-native';

const SaleByReceiptStyles = StyleSheet.create({
    dateRangePicker: { flexDirection: 'row', flex: 1, alignItems: 'center' },
});

export default SaleByReceiptStyles;
