import { StyleSheet } from 'react-native';

const ItemRankStyles = StyleSheet.create({
    dateRangePicker: { flexDirection: 'row', flex: 1, alignItems: 'center' },
});

export default ItemRankStyles;
