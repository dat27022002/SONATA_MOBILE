import { StyleSheet } from 'react-native';

const TimeBaseStyles = StyleSheet.create({
    dateRangePicker: { flexDirection: 'row', flex: 1, alignItems: 'center' },
});

export default TimeBaseStyles;
